<% @user  = User.all %>

<body>
    <div class="container">
        <div class="row">
            <%= render 'layouts/left' %>
            <div class="col-lg-6">
                <h2>投稿一覧</h2>
                投稿が一覧表示される
                <br>
                <%= paginate @boards %>
                <% @boards.each do |board|%>
                <%= render board %>
                <% end %>
            </div>
            <%= render 'layouts/right' %>
        </div>
    </div>
</body>